// 배열 생성
let arr = new Array();
let arr2 = [];
let arr3 = [1, 2, 3, 4];

//배열안에 값은 다양하게 가능
let arr4 = [1, 2, 3, true, "hello", null, undefined, () => {}, {}, []];

//배열 요소 접근, 인덱스로 해당하는 요소 접근 가능
let item1 = arr3[0];

//인덱스로 배열요소의 값을 수정도 가능
arr3[0] = "abc";
